<%= form_for(@lender) do |f| %>
  <% if @lender.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lender.errors.count, "error") %> prohibited this lender from being saved:</h2>

      <ul>
      <% @lender.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :account %><br>
    <%= f.number_field :account %>
  </div>
  <div class="field">
    <%= f.label :active %><br>
    <%= f.check_box :active %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
