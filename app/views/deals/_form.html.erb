<%= form_for(@deal) do |f| %>
  <% if @deal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@deal.errors.count, "error") %> prohibited this deal from being saved:</h2>

      <ul>
      <% @deal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :collateral %><br>
    <%= f.text_field :collateral %>
  </div>
  <div class="field">
    <%= f.label :term %><br>
    <%= f.select :term, Deal::TERM_LENGTHS %>
  </div>
  <div class="field">
    <%= f.label :active %><br>
    <%= f.check_box :active, {}, 'true', 'false' %>
  </div>  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
